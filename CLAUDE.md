# 예슬 포트폴리오

## 목적
디자인 작업물을 소개하는 개인 포트폴리오 사이트.

## 핵심 기능
- 자기소개
- 작업물 목록
- 작업물 상세
- 로그인
- 작업물 등록 / 수정 / 삭제

## 현재 상태
연습 삼아 만들고 있는 초기 단계로, 아직 위 핵심 기능 대부분은 구현되어 있지 않다.
지금까지 만든 페이지:
- `index.html` — 홈. 다른 페이지로 가는 링크 제공
- `about.html` — 소개 페이지 (자기소개 기능의 초안 성격)
- `preview.html` — Supabase `test_todos` 테이블 데이터를 목록으로 보여주는 연습 페이지 (실제 포트폴리오 기능은 아니고, 데이터 연동 연습용)
- `style.css` — 전체 페이지 공용 스타일시트. 베이비핑크를 메인 키 컬러로 사용

## 디자인 가이드
- 메인 키 컬러: 베이비핑크 (`style.css`의 `--pink-key`, `--pink-key-dark` 등 참고)
- 폰트: 깔끔한 고딕체인 'Pretendard' 사용 (jsDelivr CDN, `style.css`의 `body` `font-family` 참고). 폰트가 로드되지 않을 경우를 대비해 시스템 고딕 폰트를 fallback으로 지정해둠
- 새 페이지를 추가할 때도 `style.css`와 Pretendard CDN 링크를 함께 적용해서 톤을 통일할 것
- 공통 네비게이션은 `.nav` / `.home-link` 클래스, 콘텐츠 박스는 `.card` 클래스 사용

## 참고 사항
- 데이터 연동은 Supabase REST API를 사용 중 (`preview.html` 참고)
- 위 핵심 기능(로그인, 작업물 CRUD 등)을 구현할 때는 이 문서의 목적·기능 목록을 기준으로 방향을 잡을 것
